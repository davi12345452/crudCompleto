<%- include('partials/head.ejs') %>
<%- include('partials/navbar.ejs') %>
<div class="container">
    <div class="card mt-2">
        <div class="card-header">
            <h3><%=artigo.title%></h3>
        </div>
        <div class="card-body">
            <!--
                Aqui, ao invés de =, usamos -, o que o ejs interpreta que deve contar como um html.
                No caso, o body estava em formato HTML, trocando o = por -, possibilita que esse 
                corpo seja interpretado como código html, deixando o texto real. 
            -->
            <p><%-artigo.body%></p>
        </div>
    </div>
</div>
<%- include('partials/footer.ejs') %>